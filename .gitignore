# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local

# Editor
.vscode/
.idea/

# Java / Maven
target/
*.class

# Terraform — never commit state, real variable files, or saved plans
# (they hold secrets/IDs)
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!*.tfvars.example
tfplan
*.tfplan

# Keys / secrets
*.pem
*.key

# Generated AWS inventory (holds account id + ARNs — see scripts/aws-resources.sh)
aws-resources*.html
.playwright-mcp/